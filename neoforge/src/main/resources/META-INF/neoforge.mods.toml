modLoader = "javafml"
loaderVersion = "[1,)"
license = "MIT"

[[mods]]
modId = "emitabs"
version = "${version}"
displayName = "EMI Tabs"
description = "Adds creative-mode tabs to the top of EMI's item index so you can filter the list by creative tab."
authors = "nbrown725"
logoFile = "assets/emitabs/icon.png"
# Client-side only: skip this mod in the client/server network handshake so a
# client running it can join servers that don't have it.
displayTest = "IGNORE_ALL_VERSIONS"

[[mixins]]
config = "emitabs.client.mixins.json"

[[dependencies.emitabs]]
modId = "neoforge"
type = "required"
versionRange = "[21.1,)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.emitabs]]
modId = "minecraft"
type = "required"
versionRange = "[1.21.1,1.22)"
ordering = "NONE"
side = "CLIENT"

[[dependencies.emitabs]]
modId = "emi"
type = "required"
versionRange = "*"
ordering = "AFTER"
side = "CLIENT"
