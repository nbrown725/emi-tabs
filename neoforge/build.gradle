dependencies {
	neoForge "net.neoforged:neoforge:${rootProject.neoforge_version}"

	// EMI full jar (not the ":api" classifier) so we can mixin into EMI internals.
	modImplementation "dev.emi:emi-neoforge:${rootProject.emi_version}"
}
